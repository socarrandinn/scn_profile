// import { useMutation } from '@tanstack/react-query';
// import { ExportService } from 'modules/sales/services';

export const useExportProducts = (filters: any, onClose: () => void, search: any) => {
  // return useMutation((isVariant: boolean) => ExportService.exportToExcel(filters, search, isVariant), {
  //   onSuccess: () => {
  //     onClose?.();
  //   },
  // });
};
