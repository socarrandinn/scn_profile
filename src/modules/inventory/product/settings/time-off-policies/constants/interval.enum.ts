export enum IntervalEnum {
  month = 'month',
  day = 'day',
  year = 'year',
}

export const IntervalEnumValues = Object.values(IntervalEnum);

export enum CommonIntervalsEnum {
  hours = 'hours',
  days = 'days',
  month = 'month',
}

export const CommonIntervalsEnumValues = Object.values(CommonIntervalsEnum);
