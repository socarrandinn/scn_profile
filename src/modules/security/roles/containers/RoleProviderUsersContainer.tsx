import { memo } from 'react';
import RoleProvidersUsersTable from './RoleProvidersUsersTable';

const RoleProviderUsersContainer = () => {
  return <RoleProvidersUsersTable />;
};

export default memo(RoleProviderUsersContainer);
