import { memo } from 'react';
import { PaperTabView } from 'modules/common/components/TabsWithSections/PaperTabView';

const EmployeeFreeTime = ({ isAccount }: { isAccount?: boolean }) => {
  return <PaperTabView firsts>EmployeeFreeTime</PaperTabView>;
};

export default memo(EmployeeFreeTime);
