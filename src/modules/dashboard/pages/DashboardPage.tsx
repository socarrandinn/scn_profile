import { memo } from 'react';
import { PageLayout } from 'layouts/index';

const DashboardPage = () => {
  return <PageLayout sx={{ marginY: 3 }}>dashboard demo</PageLayout>;
};

export default memo(DashboardPage);
