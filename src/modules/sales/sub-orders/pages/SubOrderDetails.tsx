import { memo } from 'react';
import SubOrderDetailContainer from '../containers/SubOrderDetailContainer';

const SubOrderDetails = () => {
  return <SubOrderDetailContainer />;
};

export default memo(SubOrderDetails);
