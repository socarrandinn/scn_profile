import { memo } from 'react';
import PaidOrderDetailContainer from '../containers/PaidOrderDetailContainer';

const PaidOrderDetails = () => {
  return <PaidOrderDetailContainer />;
};

export default memo(PaidOrderDetails);
