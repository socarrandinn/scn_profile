import React, { memo } from 'react';
import ClientGeneralContainer from 'modules/crm/clients/containers/ClientGeneralContainer';

const ClientGeneralPage = () => {
  return <ClientGeneralContainer />;
};

export default memo(ClientGeneralPage);
