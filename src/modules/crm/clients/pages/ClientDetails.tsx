import React, { memo } from 'react';
import ClientDetailsContainer from 'modules/crm/clients/containers/ClientDetailsContainer';

const ClientDetails = () => {
  return <ClientDetailsContainer />;
};

export default memo(ClientDetails);
