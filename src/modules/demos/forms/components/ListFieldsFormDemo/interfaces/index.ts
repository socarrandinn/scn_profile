export interface IDeveloper {
  firstName: string;
  lastName: string;
  skills: string[]
}

export interface IDeveloperResult {
  _id: string,
  firstName: string;
  lastName: string;
  skills: string[]
}
