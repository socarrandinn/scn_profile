import { memo } from 'react';
import { Box } from '@mui/material';

const InputsDemosContainer = () => {
  return (
    <Box sx={{ my: 4 }}>
    </Box>
  );
};

export default memo(InputsDemosContainer);
