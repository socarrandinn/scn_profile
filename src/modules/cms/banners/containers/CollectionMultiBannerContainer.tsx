import { memo } from 'react';
import MultiBannerList from '../components/MultiBannerList/MultiBannerList';

const CollectionMultiBannerContainer = () => {
  return <MultiBannerList />;
};

export default memo(CollectionMultiBannerContainer);
